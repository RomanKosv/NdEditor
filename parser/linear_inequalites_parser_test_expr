0 /работает
-1234567890 /работает
0.0 /работает
-9876543210.0987654321 /работает
9876543210.0987654321 /работает, но точность double
+9876543210.0987654321 /работает, но точность double
.0 /работает (не парсится)
0. /работает (не парсится)
. /работает (не парсится)
x /работает
-x /работает
- x /работает
val890_adsfs /работает
x+y /работает
x-y /работает
x - y /работает
x+y-y /работает
y*2 /не работает
(y*) /не работает (парсится, но при вычислении выдает коэфицент 1, а не 2)
y*+2 /не работает
y*(+2) /не работает
y*2*2 /не работает
2*y /не работает
y/2 /не работает
2/y /не работает
1.0/0
x*y /работает (не вычисляется)
x/y /работает (не вычисляется)
2*x+y/3 /не работает
(x/1) /работает
(x*1) /работает
(x+1) /работает
(x+y) /работает
(x+1)*(3/2-1)/не работает (парсится, но при вычислении выдает коэфицент 1, а не 0.5)
x<y /работает
x<=y /работает
x>y /работает
x>=y /работает
x=y /работает
x!=y /работает
x-y*1>0 /работает
!(x=y) /работает
!(x<y) /работает
(x<z) /работает
x<z&(!x>y|x<y)|y=z /работает
fun() /парсится
fun(x) /парсится
fun(x,y,z) /парсится
fun(x+y)/не работает, считывается как переменная
fun((x+y)) /парсится
fun(x|y)/не работает, считывается как переменная
fun(x)*1/работает
